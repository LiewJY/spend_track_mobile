import 'package:flutter/widgets.dart';

class YearlyTransactionScreen extends StatelessWidget {
  const YearlyTransactionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('yearly  ');
  }
}